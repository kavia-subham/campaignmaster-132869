/**
 * PUBLIC_INTERFACE
 * Shared type declarations for the frontend.
 * Using TypeScript declaration file to provide typing hints for JS code editors.
 */

export type User = {
  id: string;
  name: string;
  email: string;
};
