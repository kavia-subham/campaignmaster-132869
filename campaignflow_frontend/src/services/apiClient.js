/// PUBLIC_INTERFACE
/** Minimal API client placeholder; to be expanded with fetch wrappers and interceptors.
 * Do not implement external dependencies here yet.
 */
export const apiClient = {
  async get(_path) {
    // Placeholder stub. Implement actual calls when backend is available.
    return { ok: true, data: null };
  },
  async post(_path, _body) {
    return { ok: true, data: null };
  },
};
