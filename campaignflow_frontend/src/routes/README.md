# Routes

This folder centralizes route constants and, in future steps, will include the router configuration.

- Use `routes/index.js` to export route constants and lazy loaders.
- Actual routing (react-router) is not implemented yet by design for this step.
